// SDGGameplay — Stores, Events, ECS systems.
// Real content arrives in Phase 1 (POC).
import SDGCore

public enum SDGGameplayModule {
    public static let version = "0.0.0"
    public static let coreVersion = SDGCoreModule.version
}
