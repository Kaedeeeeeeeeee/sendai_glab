// SDGPlatform — platform services (persistence, audio, input).
// Depends on SDGCore only. Real services arrive in P0-T2 onwards.
import SDGCore

public enum SDGPlatformModule {
    public static let version = "0.0.0"
}
